object Polymorphism {
  /**
   * Cons-Lists
   * - fundamental DS in many functional languages is the immutable linked list. It is constructed from
   *    - Nil (the empty list)
   *    - Cons (a cell containing an element and the remainder of the list)
   */

  /**
   * like java, the types are used by the compiler, but not used in actual execution (type erasure)
   */
}