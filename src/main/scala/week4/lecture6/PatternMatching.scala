package week4.lecture6

/**
 * Created by hamishdickson on 01/06/2014.
 *
 * pattern matching is similar to a switch statement in java/c
 */
class PatternMatching {
  /*def eval(e: Expr): Int = e match {
    case Number(n) => n
    case Sum(e1, e2) => eval(e1) + eval(e2)
  }*/
}
